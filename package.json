{
    "name": "ftl",
    "version": "0.5",
    "keywords": [
        "templating",
        "dom"
    ],
    "license": "MIT",
    "author": "rferranti",
    "contributors": ["Petro36", "shun17", "Grogdunn"],
    "main": "index.mjs",
    "scripts": {
        "clean": "rimraf dist && npm prune",
        "initialize": "npm install",
        "compile": "peggy src/expressions-parser.peggy -o src/expressions-parser.mjs --format es && peggy src/expressions-textnode-parser.peggy -o src/expressions-textnode-parser.mjs --format es",
        "test": "mocha --recursive -R spec",
        "package": "npx rollup --config rollup.conf.js"
    },
    "devDependencies": {
        "@rollup/plugin-node-resolve": "^13.3.0",
        "jsdom": "^20.0.0",
        "mocha": "^9.2.2",
        "peggy": "^2.0.1",
        "rollup-plugin-terser": "7.0.2",
        "terser": ">=5.14.2"
    }
}
